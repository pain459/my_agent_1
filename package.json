{
  "name": "my-agent-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local web agent workspace that talks with a user and calls the OpenAI API.",
  "scripts": {
    "start": "python3 py_backend/server.py",
    "web": "python3 py_backend/server.py",
    "training:export": "python3 -m py_backend.tools training-export",
    "check": "python3 -m py_compile py_backend/server.py py_backend/tools.py && node --check public/app.js && node --check public/admin.js"
  },
  "engines": {
    "node": ">=20"
  }
}
